#include <stdio.h>

/**
 * main - a program that prints it's name
 * @argc: argument count
 * @argv: argument size
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
